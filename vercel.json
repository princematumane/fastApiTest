{
    "devCommand": "uvicorn main:app --host 127.0.0.1 --port 8000",
    "build": 
        {
            "src": "app/index.py",
            "use": "@vercel/python"
        }
    ,
    "routes": [
        {
            "src": "/(.*)",
            "dest": "app/index.py"
        }
    ]
}